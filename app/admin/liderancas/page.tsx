import { cookies } from 'next/headers';import { redirect } from 'next/navigation';import { verifySession } from '@/lib/auth';import { db } from '@/lib/db';
const seed=[{slug:'raimundo-verissimo',name:'Raimundo Veríssimo'},{slug:'maria-oliveira',name:'Maria Oliveira'},{slug:'carlos-santos',name:'Carlos Santos'}];
export default async function Liderancas(){if(!await verifySession((await cookies()).get('mobiliza_session')?.value))redirect('/admin/login');let leaders:any[]=seed;try{const r=await db.query('SELECT slug,name FROM leaders WHERE active=true ORDER BY name');if(r.rows.length)leaders=r.rows}catch{}return <main className="dash-content"><div className="overview-title"><div><p>Gestão de equipes</p><h2>Lideranças</h2><p>Crie, compartilhe ou desative links individuais.</p></div></div><section className="panel" style={{marginTop:24}}><div className="panel-heading"><h3>Nova liderança</h3></div><form action="/api/leaders" method="post" style={{display:'grid',gridTemplateColumns:'1fr 1fr auto',gap:12,padding:20}}><input name="name" required placeholder="Nome completo"/><input name="slug" required placeholder="slug-do-link"/><button className="submit-button" type="submit" style={{height:46}}>Criar</button></form></section><section className="panel" style={{marginTop:18}}><div className="panel-heading"><h3>Links ativos</h3></div>{leaders.map((l:any)=><div key={l.slug} style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:'16px 20px',borderBottom:'1px solid var(--line)'}}><div><strong>{l.name}</strong><div><code>/lideranca/{l.slug}</code></div></div><div style={{display:'flex',gap:12}}><a href={`/admin/relatorio/${l.slug}`}>PDF</a><form action="/api/leaders" method="post"><input type="hidden" name="action" value="delete"/><input type="hidden" name="slug" value={l.slug}/><button type="submit" style={{color:'#b91c1c',border:0,background:'none'}}>Excluir</button></form></div></div>)}</section></main>}
