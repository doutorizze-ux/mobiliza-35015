import { cookies } from 'next/headers';import { redirect } from 'next/navigation';import { verifySession } from '@/lib/auth';
export default async function Liderancas(){if(!await verifySession((await cookies()).get('mobiliza_session')?.value))redirect('/admin/login');return <main className="dash-content"><h2>Nova liderança</h2><p>Preencha os dados para criar um formulário individual.</p><form action="/api/leaders" method="post" className="contact-form" style={{margin:0,maxWidth:560}}><label>Nome<input name="name" required placeholder="Ex.: João da Silva"/></label><label>Slug do link<input name="slug" required placeholder="joao-da-silva"/></label><button className="submit-button" type="submit">Criar liderança</button></form></main>}
